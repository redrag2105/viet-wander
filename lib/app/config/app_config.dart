import 'package:latlong2/latlong.dart';

class AppConfig {
  // Tâm bản đồ mặc định (Gần Đà Nẵng)
  static const LatLng defaultMapCenter = LatLng(16.042078, 106.206230);
  static const double defaultMapZoom = 6.0;
}
